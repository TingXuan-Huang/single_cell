# Pinned for UW Hyak reproducibility. Adjust torch CUDA tag for cluster GPU.
torch>=2.1,<3.0
numpy>=1.24,<2.0
pandas>=2.0
scipy>=1.11
anndata>=0.10
scanpy>=1.10
scikit-learn>=1.3
pyarrow>=14.0
pyyaml>=6.0
tabulate>=0.9
tqdm>=4.65
wandb>=0.16
matplotlib>=3.7
seaborn>=0.13

# Allen access (install with `pip install -e .[allen]`)
# abc-atlas-access

# Dev
pytest>=7.4
pytest-cov>=4.1
